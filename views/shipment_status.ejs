<!DOCTYPE html>
<html lang="en">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>Shipment Status</title>
    <script src="https://cdn.tailwindcss.com"></script>
</head>
<body class="bg-gray-100 flex flex-col items-center justify-center min-h-screen p-6">
    <div class="bg-white p-6 rounded-lg shadow-md w-full max-w-2xl">
        <h2 class="text-2xl font-bold mb-4 text-center">Shipment Status</h2>
        <ul class="space-y-4">
            <% notifications.forEach(notification => { %>
                <li class="p-4 border rounded-lg shadow-md">
                    <strong>Shipment ID:</strong> <%= notification.shipmentId %><br>
                    <strong>Status:</strong> <%= notification.status %><br>
                    <strong>Message:</strong> <%= notification.message %><br>
                    <strong>Created At:</strong> <%= new Date(notification.createdAt).toLocaleString() %><br>
                </li>
            <% }); %>
        </ul>
    </div>
</body>
</html>