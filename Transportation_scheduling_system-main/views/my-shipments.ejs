<!DOCTYPE html>
<html lang="en">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>My Shipments - Transport Scheduler</title>
    <link href="https://cdn.jsdelivr.net/npm/bootstrap@5.3.0/dist/css/bootstrap.min.css" rel="stylesheet">
    <link href="https://cdnjs.cloudflare.com/ajax/libs/font-awesome/6.4.0/css/all.min.css" rel="stylesheet">
    <style>
        body { 
            background: linear-gradient(135deg, #667eea 0%, #764ba2 100%); 
            min-height: 100vh; 
        }
        .shipment-card { 
            transition: transform 0.2s; 
        }
        .shipment-card:hover { 
            transform: translateY(-5px); 
        }
        .status-badge {
            font-size: 0.75rem;
            padding: 0.25rem 0.5rem;
        }
    </style>
</head>
<body>
    <div class="container mt-4">
        <div class="row">
            <div class="col-12">
                <div class="d-flex justify-content-between align-items-center mb-4">
                    <div>
                        <h1 class="text-white">📦 My Shipments</h1>
                        <p class="text-white-50">Track and manage all your shipments</p>
                    </div>
                    <a href="/user-dashboard" class="btn btn-light">
                        <i class="fas fa-arrow-left me-2"></i>Back to Dashboard
                    </a>
                </div>
            </div>
        </div>

        <% if (shipments.length === 0) { %>
            <div class="row">
                <div class="col-12">
                    <div class="card">
                        <div class="card-body text-center p-5">
                            <i class="fas fa-box-open fa-4x text-muted mb-3"></i>
                            <h4>No shipments found</h4>
                            <p class="text-muted">You haven't created any shipments yet.</p>
                            <a href="/user-dashboard" class="btn btn-primary">
                                <i class="fas fa-plus me-2"></i>Create Your First Shipment
                            </a>
                        </div>
                    </div>
                </div>
            </div>
        <% } else { %>
            <div class="row g-4">
                <% shipments.forEach(function(shipment) { %>
                    <div class="col-md-6 col-lg-4">
                        <div class="card shipment-card h-100">
                            <% if (shipment.photo) { %>
                                <img src="<%= shipment.photo %>" class="card-img-top" alt="Shipment" style="height: 200px; object-fit: cover;">
                            <% } %>
                            <div class="card-body">
                                <h5 class="card-title">
                                    <i class="fas fa-box text-primary me-2"></i>
                                    <%= shipment.goodsDescription || 'Shipment' %>
                                </h5>
                                
                                <div class="mb-2">
                                    <strong>📍 Route:</strong><br>
                                    <small class="text-muted">
                                        <i class="fas fa-map-marker-alt text-success me-1"></i>
                                        <%= shipment.pickup || shipment.location %>
                                        <br>
                                        <i class="fas fa-arrow-down mx-3"></i>
                                        <br>
                                        <i class="fas fa-map-marker-alt text-danger me-1"></i>
                                        <%= shipment.destination %>
                                    </small>
                                </div>

                                <p class="card-text">
                                    <strong>📅 Date:</strong> 
                                    <%= new Date(shipment.dateTime).toLocaleDateString() %>
                                    <br>
                                    <strong>🚛 Vehicle:</strong> 
                                    <%= shipment.vehicleType %>
                                    <br>
                                    <% if (shipment.routeCost) { %>
                                        <strong>💰 Cost:</strong> ₹<%= shipment.routeCost %>
                                        <br>
                                    <% } %>
                                    <% if (shipment.routeDistance) { %>
                                        <strong>📏 Distance:</strong> <%= shipment.routeDistance %> km
                                    <% } %>
                                </p>

                                <div class="mb-3">
                                    <span class="badge status-badge <%= shipment.status === 'pending' ? 'bg-warning text-dark' : (shipment.status === 'approved' ? 'bg-success' : 'bg-danger') %>">
                                        <%= shipment.status.toUpperCase() %>
                                    </span>
                                </div>

                                <div class="alert alert-info py-2" role="alert">
                                    <small>
                                        <strong>🔍 Tracking ID:</strong><br>
                                        <code class="user-select-all"><%= shipment._id %></code>
                                    </small>
                                </div>
                            </div>
                            <div class="card-footer">
                                <a href="/Real_tracker?shipmentId=<%= shipment._id %>" class="btn btn-primary w-100">
                                    <i class="fas fa-map-marker-alt me-2"></i>Track This Shipment
                                </a>
                            </div>
                        </div>
                    </div>
                <% }); %>
            </div>
        <% } %>
    </div>

    <script src="https://cdn.jsdelivr.net/npm/bootstrap@5.3.0/dist/js/bootstrap.bundle.min.js"></script>
</body>
</html>
